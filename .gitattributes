# Model .joblib files are no longer tracked in Git — they're hosted as
# GitHub Release assets and downloaded at runtime by the AI services.
# See exercise_selector.py / weight_predictor.py for the URL env vars.
